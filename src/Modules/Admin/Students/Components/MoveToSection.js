import React from 'react';

const MoveToSection = ({ studentId }) => {
  return (
    <div>
      <h2>Move to Section</h2>
      <p>Student ID: {studentId}</p>
      {/* Additional move to section logic */}
    </div>
  );
};

export default MoveToSection;
